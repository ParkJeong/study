#include <stdio.h>
/*
int main(void) {
	
	// Q1
	int x1, y1, x2, y2;

	printf("좌 상단의 x, y 좌표: ");
	scanf_s("%d", &x1);
	scanf_s("%d", &y1);
	
	printf("우 상단의 x, y 좌표: ");
	scanf_s("%d", &x2);
	scanf_s("%d", &y2);

	printf("두 점이 이루는 직사각형의 넓이는 %d입니다.\n\n", (x2-x1) * (y2-y1));


	// Q2
	double x, y;

	printf("두 수를 입력해주세요.\n");
	scanf_s("%lf %lf", &x, &y);

	printf("%f + %f = %f\n", x, y, x + y);
	printf("%f - %f = %f\n", x, y, x - y);
	printf("%f * %f = %f\n", x, y, x * y);
	printf("%f / %f = %f\n", x, y, x / y);

	// Q3
	// 아스키 코드를 구성하는 값의 범위는 0~127
	// 대문자 A에서부터 대문자 Z까지 값이 1씩 증가한다.
	// 알파벳 대문자와 소문자간의 값의 차이는 32이다.
	
	// Q4
	int num;
	
	printf("숫자를 입력해주세요.\n");
	scanf_s("%d", &num);

	printf("해당 정수의 아스키 코드문자: %c\n", num);
	
	// Q5
	char input;

	printf("문자를 입력해주세요.\n");
	scanf_s("%c", &input);

	printf("해당 문자의 아스키 코드값: %d\n", input);

	return 0;
}
*/